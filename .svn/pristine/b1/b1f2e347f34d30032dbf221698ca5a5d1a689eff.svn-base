package com.herotculb.qunhaichat.weixin.gailan;

import java.util.Map;

import com.herotculb.qunhaichat.widget.LoadingDialog;

import cn.pedant.SweetAlert.SweetAlertDialog;
import android.content.Context;
import android.os.Handler;

public class WeixinMenuManagerAddHandler extends Handler {
	LoadingDialog sDialog;
	Context context;
	public WeixinMenuManagerAddHandler(LoadingDialog dialog2, Context context) {
		super();
		this.sDialog = dialog2;
		this.context = context;
	}
	@Override
	public void handleMessage(android.os.Message msg) {
		super.handleMessage(msg);
		Map<String, Object> map = (Map<String, Object>) msg.obj;
		sDialog.hide();
		sDialog.dismiss();
	}
}
